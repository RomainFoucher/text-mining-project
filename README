Dot to png
==========
dot -Tpng tmp.dot > tmp.png
feh tmp.png&



Dictionary grammar
==================
() is a group
? is zero or one
+ is one or more
* is zero or more
-------------------

file =
    table trie_node


table =
    table_size chars

table_size = uint32
chars = (char)* '\0'


trie_node =
    end_of_word frequency nb_children (data)*

end_of_word = bool
frequency = uint_32
nb_children = uint8_t

data =
    next_char index len trie_node

next_char = char
index = uint32_t
len = uint8_t